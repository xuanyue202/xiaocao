# 方正证券量化平台 OpenCLI 本地配置模板。
# 复制为 foundersc-quant.env 后填写；真实密码不要写回本文件。
# 当前 OpenCLI 模板实际只读取这个页面根地址；其余账户、凭证和写入
# 开关必须由受审查的调用方控制，不能把未消费的环境变量误当成安全门。

FZZQ_QUANT_BASE_URL=https://quant.foundersc.com/qtassets/dist/index.html#/user/login

# 凭证不得进入项目 env；实际登录仍需通过受控的 OpenCLI/Keychain 流程。
# 当前仅提供 read-only probe/prepare/reconcile/recover 模板，没有 submit 路径。
# 真实资金、账户绑定、确认模式和写入授权不由此文件声明或授权。
