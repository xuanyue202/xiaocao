[pytest]
testpaths = tests
markers =
    app_simulation: APP-specific regression/simulation; weekends or weekdays outside 09:00-15:00 Asia/Shanghai only
    e2e: live end-to-end tests against the Xiaocao API
addopts = -ra
