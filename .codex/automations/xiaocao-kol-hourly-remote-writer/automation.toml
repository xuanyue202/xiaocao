version = 1
id = "xiaocao-kol-hourly-low-bandwidth-operation"
kind = "cron"
name = "xiaocao KOL hourly remote writer"
prompt = "使用本机 `kol-intelligence` skill，并完整读取 `references/hourly-remote-writer.md`。这是 `MacBook-Pro-6.local` 上唯一 KOL remote writer 和唯一 KOL 写入入口；跨机 WeChat capture 只作为 credential-free handoff capsule 输入。\n\n启动任何 KOL runner 或读取 mailbox/status/convergence 前，只运行一次 `CODEX_HOME=/Users/xuanyue202/.codex CODEX_AUTOMATION_ID=xiaocao-kol-hourly-low-bandwidth-operation CODEX_REMOTE_HOST=MacBook-Pro-6.local node scripts/codex_peer_gate.js`。只按结构化 gate_result 分支：pass 才继续；no_op 静默结束；repair_required 或无有效结果时先修 gate surface，不产生业务副作用。Gate 通过后，有 exact continuation 就 narrow resume，否则只启动一次 `PYTHONPATH=src .venv/bin/python scripts/kol_daily.py run`；不得第二次 full sweep。\n\n执行 Agent 只负责 gate、handoff、证据取得、确定性 prepare/dispatch、等待、对账、校验、上传和外部效果。semantic-input 必须按 `references/semantic-model-routing.md` 和 `.codex/skills/kol-intelligence/config/semantic-analyst.json` 原样 dispatch；洞察 Agent 完整阅读来源、双遍抽取和覆盖审计。每条可能影响 Book B 决策的纵向观点必须直接包含 horizon、triggers、falsifiers、uncertainties、证据引用和显式当前性 evaluation。经校验的同一 semantic bundle 原子发布报告、viewpoint 和初始 viewpoint_evaluation；维护追加 evaluation/relation。LiangHui 是唯一来源语义 SSOT。不得再创建 `kol-source-preparation` 笔记、逐对象准备包、opening draft，或要求交易消费者重新提炼正文。\n\n每个对象仍须满足灰常亮、提醒、Book KOL-US、knowledge、ack、claim=receipt、uncertain=0 和 writer_progress terminal。任何 Agent-owned 异常进入 exact-object 最小回归、修复、定向验证和 narrow resume；认证、验证码、同意或不可对账外部效果才是用户阻塞。空队列静默；非空结果逐对象说明并以 `对象 | 状态 | 说明` 表格收尾。"
status = "ACTIVE"
rrule = "RRULE:FREQ=DAILY;BYHOUR=8,10,12,14,17,18,21;BYMINUTE=30"
model = "gpt-5.6-sol"
reasoning_effort = "medium"
notification_policy = "failed_runs_only"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1785628290778
updated_at = 1789350688655
