version = 1
id = "xiaocao-intraday-monitor-1455"
kind = "cron"
name = "xiaocao intraday monitor closing discipline"
prompt = """TIME-CRITICAL 14:55. Use `xiaocao-trading`. After one brief skill-use commentary, the first shell tool call must be `bash scripts/book_b_live_closing_startup.sh`; do not plan, inspect, or delegate first. This is the one read-only startup batch: it reads `/Users/xuanyue202/.codex/automations/xiaocao-intraday-monitor-1455/memory.md`, `.codex/skills/xiaocao-trading/SKILL.md`, `.codex/skills/xiaocao-trading/references/automation-intraday.md`, `.codex/skills/xiaocao-trading/references/book-b-live-repair.md`, and `.codex/skills/xiaocao-trading/references/kol-trading-judgment.md`, folds any required narrow `MEMORY.md` lookup into that batch, then in the same shell tool call runs `PYTHONPATH=src .venv/bin/python scripts/book_b_live_intraday.py --date today --phase closing --execute-sells`; do not return control to the model between the reads and that command. This is the complete pre-command set: do not make a separate discovery/count/list/read call, do not open sibling skill references before the live command, and do not inspect journals, Git, KOL, paper, status, data-doctor, broker state, or other diagnostics first.

This is the single closing pass. Start immediately when woken and do not wait. The live command above is the first executable business command and runs exactly once. After it terminates, run `PYTHONPATH=src .venv/bin/python scripts/live_monitor.py --execute-sells` exactly once, then reconcile paper and real-capital branches separately. Only 14:55:00-14:56:59 China time may hand off a soft exit. Account, owned-lot, T+1, liquidity, capital-key, durable-claim, and UNKNOWN/no-retry gates remain unchanged; an expired window never permits a late SELL or second live run.

Founder funds use the settlement-aware contract: normally cash balance + securities value = total assets; after a broker-proved same-day SELL, available cash may close assets only when withdrawable cash <= cash balance <= available cash. Persist the field and fail closed otherwise; this never expands BUY authority.

For any non-normal live result, the started task owns repair; do not defer to the next Automation. Preserve receipt/state/fingerprint, add a tight red test, rank 3-5 falsifiable hypotheses, patch the smallest owning boundary, run focused and live-safety regressions, commit/push only the repair allowlist, and take only an exact narrow resume. Broker uncertainty is reconcile-only. A repeated same failure fingerprint requires a deeper repair. Finish with terminal reconciliation and a 5 Why.

Use only an already-published fresh bounded KOL packet at this gate; no review wait. Keep this Automation's model. If semantic judgment is actually required after both branches, delegate only that judgment to `gpt-6-astra`/`xhigh`, save accepted dispatch, provide complete context, and independently review before `scripts/kol_trading_decision.py publish`. Only validated v2 `kol-xiaocao` explicit mode-follow may precede rotation inside frozen same-day non-BJSE candidates; it may restore COLD, never UNKNOWN, capital keys, frozen evidence, or durable intent. No capital/leverage/order authority is added. Inspect KOL/risk consumption only after both branches, without replay."""
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=14;BYMINUTE=55"
model = "gpt-5.6-sol"
reasoning_effort = "xhigh"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1785639828046
updated_at = 1788764509539
