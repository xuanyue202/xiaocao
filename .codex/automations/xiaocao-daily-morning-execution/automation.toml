version = 1
id = "xiaocao-daily-morning-execution"
kind = "cron"
name = "xiaocao daily morning execution"
prompt = "Use the local `xiaocao-trading` skill and read `automation-morning.md`, `kol-trading-judgment.md` and `kol-source-preparation.md`. Start `bash scripts/auto_daily.sh morning-execute` exactly once; never rerun `live_recommend`. Keep that original shell alive through its bounded structured-review rendezvous, consume only the dated frozen queue, write `agent_intelligence_review` records without keyword scoring, let timeout use base picks, then verify and report Book B and Book T separately from their own receipts. A Book-B freeze or record failure must not suppress the independent Book-T branch.\n\nThe remote writer owns full reading and independent source-level review for new or changed KOL reports. During the existing rendezvous, build the cache-only context and run `kol_trading_preparation.py status`. For `prepared`, reuse the reviewed packet. For `source_revalidation_required`, verify exact missing manifests and reuse `reusable_path`; do not open unchanged report bodies. For `source_analysis_required`, a missing packet, or changed evidence without an approved packet, record supporting degradation and let the original shell continue with the deterministic baseline. Do not take over source analysis, repeat the 09:00 live task or read live account state. Any Astra xhigh worker here owns paper current-applicability adaptation only, using the packet plus the exact paper freeze/account/risk/current facts. The parent independently reviews that mapping and counterevidence, not the unchanged source corpus. Publish only `runtime=paper` through `kol_trading_decision.py` before the existing barrier closes. Never claim both runtimes, backdate a late decision or replay a completed paper run.\n\nReport actual ledger consumption, fills/skips, cash/equity and supporting fallback. API code 990502 is authentication failure, not an empty candidate set."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=9;BYMINUTE=25"
model = "gpt-6-astra"
reasoning_effort = "medium"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1785639807924
updated_at = 1789612092202
