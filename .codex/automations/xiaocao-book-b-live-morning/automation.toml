version = 1
id = "xiaocao-book-b-live-morning"
kind = "cron"
name = "xiaocao Book B live morning"
prompt = "Use the local `xiaocao-trading` skill/project conventions. Run `PYTHONPATH=src .venv/bin/python scripts/book_b_live_morning.py --date today --route native-app` exactly once as the separate Book B Founder live-morning seam. Use only the Founder macOS App for account binding, positions, orders, trades, funds, allocation, prepare, submit and reconciliation; do not initialize or use OpenCLI inside this route. Consume only the dated deterministic freeze and broker-sourced allocation facts; never run or wait for `morning-execute`, and never read or write simulated fills or canonical paper ledgers. Start the sanitized Keychain capital-runtime plus native App/account/query/allocation/prepare preflight at 09:20, keep the verified App session warm, and let the deterministic 09:30 submit floor control any BUY. Never run `configure_live_capital_keychain.py` or `authorize_live.py`; Automation may only consume the pre-existing Keychain runtime and scoped authorization. Keep durable live intent/reconcile state under `output/live/book_b_live_execution/`. Require zero pre-existing exact code/side/price/quantity orders before prepare and exactly one new numeric order id after the single submit; bind fills by order id, code and side. Fail closed on malformed, missing, duplicate or unknown native readback with no submit retry. Record web authentication as not used and native mock restoration as not applicable. Report account binding, allocation, market guard, safety-key, PassGuard, submit receipt, order-id, cancellation/retry, or reconcile gaps; never infer a broker submit/fill from a prepared form and never repeat a final submit click. After any non-normal result, read `references/book-b-live-repair.md`: the started task owns repair; do not defer to the next Automation. Classify a local defect as `repair_required`; preserve the exact receipt and durable state, add a tight red test, list 3–5 falsifiable hypotheses, patch the smallest root boundary, run focused and live-safety regression tests, commit and push only the repair allowlist, then use the exact narrow resume authorized by durable state until terminal evidence. A durable claim or possible broker side effect is reconcile-only: repeat reads if allowed but never prepare, Return, confirm, submit, cancel or replace again. Only authentication, CAPTCHA, consent, macOS permission, a user-only fact or an externally uncertain side effect may become user-action-required. After the terminal outcome, record a 5 Why plus failure fingerprint and prevention in Automation memory; if the same failure fingerprint recurs, treat the prior prevention as failed and repair a deeper boundary before returning."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=9;BYMINUTE=20"
model = "gpt-5.3-codex"
reasoning_effort = "high"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1787482901936
updated_at = 1787491428039
