version = 1
id = "xiaocao-book-b-live-morning"
kind = "cron"
name = "xiaocao Book B live morning"
prompt = "Use the local `xiaocao-trading` skill and read `automation-morning.md`, `kol-trading-judgment.md`, `kol-source-preparation.md` and `book-b-live-repair.md`. Start exactly one original live runner with short yield: `PYTHONPATH=src .venv/bin/python scripts/book_b_live_morning.py --date today --route native-app`. Preserve its 2100-second freeze wait. In parallel run exactly one authentication-only market-data probe. The 09:23 producer owns the recommendation/freeze; this task never runs or waits for paper `morning-execute`. After the initial native preflight, leave the App untouched until the runner's 09:24 recovery and heartbeat path. Preserve all account, allocation, market, capital-key, durable-claim, UNKNOWN/no-retry and same-plan recovery rules.\n\nThe remote writer is the sole owner of full reading and independent source-level review for new or changed KOL reports. Build the cache-only context and run `kol_trading_preparation.py status`. For `prepared`, reuse the reviewed packet. For `source_revalidation_required`, verify only exact missing manifests and reuse `reusable_path`; do not open unchanged report bodies. For `source_analysis_required`, a missing packet, or changed evidence without an approved packet, record supporting degradation and use the deterministic baseline; do not take over source analysis or start another writer. Near 09:22 inspect only the approved delta. Any Astra xhigh worker in this task owns live current-applicability adaptation only, using the packet plus the exact live freeze/account/risk/current facts. The parent independently reviews that mapping and counterevidence, not the unchanged source corpus. Publish only `runtime=live` through `kol_trading_decision.py` within the existing bounded rendezvous; never extend freshness, backdate, replay the runner or assert paper evidence.\n\nDuring 09:25-09:30, finish the original live execution/reconciliation path first. Repair only the exact AX/code failure, minimally validate and narrow-resume the durable plan; any possible broker write is reconcile-only. Report actual Book B strategy cash/NAV, accepted orders/fills/UNKNOWN, terminal receipt, timing and supporting degradation. After terminal readback, perform only scoped root-cause validation and preserve unrelated WIP."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=9;BYMINUTE=0"
model = "gpt-6-astra"
reasoning_effort = "medium"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1787482901936
updated_at = 1789612092115
