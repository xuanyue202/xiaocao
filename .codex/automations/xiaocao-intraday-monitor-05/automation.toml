version = 1
id = "xiaocao-intraday-monitor-05"
kind = "cron"
name = "xiaocao intraday monitor sparse"
prompt = "Use the local xiaocao-trading skill and its kol-trading-judgment and intraday references. First business command: `PYTHONPATH=src .venv/bin/python scripts/kol_trading_tick.py poll`. This Automation runs only the four original sparse checkpoints: 10:25, 10:55, 13:25 and 13:55 China time. On this `/Users/xuanyue202` remote host, `xiaocao-kol-hourly-low-bandwidth-operation` is the sole KOL writer and sole KOL write ingress; a WeChat capture produced on another host is only an upstream credential-free handoff capsule. Never create, enable, run, or substitute a local-capture Automation here. This task discovers no KOL source and consumes only completed production publication receipts at the checkpoints; it never creates a five-minute polling loop. A no_op ends silently before journal, MCP, model delegation or broker read. For reconcile_required / RUNNING_CLAIM, use the returned owner_thread_id for exactly one task-status read. If that owner is active, end immediately without waiting or reading business ledgers, broker state, source context, or models. If the owner is terminal and the claim is already cleared, end. Only when the owner is terminal or unavailable and the exact claim remains may this task reconcile durable receipts; never clear a claim by time or repeat an unknown business effect. On run, preserve the exact token and frozen fingerprints. Run the normal paper Book B monitor once and wait for terminal result, then run `PYTHONPATH=src .venv/bin/python scripts/book_b_live_intraday.py --date today --phase sparse --execute-sells` once and reconcile terminal evidence. Keep paper and live separate; preserve T+1, fresh quotes, ownership, two-key native execution, hard/structured-event/verified-KOL exit authority and ordinary soft-exit deferral. `current_checks` are dated audit context without an independent fixed TTL; every actual consumer still refreshes its own quote, account, lot/T+1, liquidity and capital gates. If need_semantic_review is true, it must come from a new completed production source fingerprint or an explicitly expired decision, never current-check age alone. After necessary exit/reconcile work, refresh the exact relevant registered source context, delegate semantic judgment only to `gpt-6-astra` with `xhigh`, independently review, and publish through `scripts/kol_trading_decision.py publish`; use an explicit valid_until of at most 24 hours and normally no later than the current trading-day boundary. Reuse hash-bound historical evidence when sources are unchanged; do not repeat full historical loading or analysis merely because time passed. A new decision is consumed at the next legal checkpoint, never by rerunning this one. Ack only the exact tick token using `scripts/kol_trading_tick.py ack --token <returned-token> --outcome completed|degraded` after business processes and evidence are terminal; later arrivals remain pending. Stay quiet on unchanged/non-actionable state; report only material execution/risk, degraded coverage, unreconciled failure, or required user action. Only a validated v2 kol-xiaocao explicit mode-follow may precede normal mode rotation inside the same-day frozen Book-B candidate set; it may restore COLD but never UNKNOWN, BJSE, a non-frozen code, capital keys, the frozen capsule, or an existing durable intent. No new Automation, broker writer, capital, leverage, or live order is authorized.\n\nFollow the current intraday reference for deferred_buy_plan_ids: a fully bound, proven unclaimed BUY stays with its original morning owner and does not block existing owned-lot monitoring or authorized SELLs. Do not take it over or close it merely to run the monitor. Claims, uncertain effects and pending SELLs still reconcile first; capital reservations and final settlement guards remain. Use the native AX reference short readiness checks; no long stable-empty wait."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=10,13;BYMINUTE=25,55"
model = "gpt-5.6-sol"
reasoning_effort = "high"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1785639827968
updated_at = 1789217982557
