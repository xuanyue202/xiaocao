version = 1
id = "xiaocao-intraday-monitor-05"
kind = "cron"
name = "xiaocao intraday monitor sparse"
prompt = "Use the local `xiaocao-trading` skill and read `automation-intraday.md`, `kol-trading-judgment.md` and `kol-source-preparation.md`. First business command: `PYTHONPATH=src .venv/bin/python scripts/kol_trading_tick.py poll`. This Automation runs only at 10:25, 10:55, 13:25 and 13:55 China time. `no_op` ends silently before journal, broker, source or model reads. For `reconcile_required / RUNNING_CLAIM`, read the returned owner task once; an active owner ends this duplicate immediately. Never clear a claim by age or repeat an unknown effect.\n\nOn `run`, retain the exact token/fingerprints. Run the paper Book B monitor once, then the live sparse checkpoint once, and reconcile both terminal receipts. Keep paper/live, T+1, ownership, liquidity, capital-key, hard/event/KOL-exit and soft-exit deferral rules separate. If `need_semantic_review=false`, do no KOL source/context/model work. If true, build the exact context and run `kol_trading_preparation.py status`. Reuse a `prepared` packet or exact manifest-only `source_revalidation_required` plus `reusable_path`; do not open unchanged report bodies. `source_analysis_required`, a missing packet or unreviewed changed evidence degrades this tick and remains remote-writer work; do not recreate source analysis. An Astra xhigh worker owns current-applicability adaptation only, using the approved packet/delta plus both runtime facts. The parent independently reviews that mapping and counterevidence. Publish only for runtimes actually checked, for the next legal tick, through `kol_trading_decision.py`; never rerun this checkpoint.\n\nAck the exact token `completed|degraded` only after both business branches and any authorized supporting work are terminal. Later arrivals remain pending. Stay quiet on unchanged state and preserve deferred BUY ownership and every UNKNOWN/reconcile boundary."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=10,13;BYMINUTE=25,55"
model = "gpt-5.6-sol"
reasoning_effort = "high"
execution_environment = "local"
target = { type = "project", project_id = "c5acdaa7-f8a7-42d4-96fe-bf979a6b7415" }
cwds = ["/Users/xuanyue202/Documents/project/xiaocao"]
created_at = 1785639827968
updated_at = 1789612092374
